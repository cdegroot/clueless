defmodule Clueless.VoteTest do
  use Clueless.ModelCase

  # Empty. The generated attribute validations tests are a tad useless if there are none.
  # If attributes are added, also re-add the invalid data controller tests.
end

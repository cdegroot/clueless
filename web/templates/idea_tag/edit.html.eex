<h2>Edit idea tag</h2>

<%= render "form.html", changeset: @changeset,
                        action: idea_tag_path(@conn, :update, @idea_tag) %>

<%= link "Back", to: idea_tag_path(@conn, :index) %>

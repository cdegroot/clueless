<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="">

    <title>OAuth2/Phoenix!</title>
    <link href="//maxcdn.bootstrapcdn.com/font-awesome/4.2.0/css/font-awesome.min.css" rel="stylesheet">
    <link rel="stylesheet" href="<%= static_path(@conn, "/css/app.css") %>">
  </head>

  <body>

    <div class="jumbotron">
      <span class="logo"></span>
      <h2>OAuth2/Phoenix Example Application!</h2>
      <p class="lead">
        This is an application to show an example of how to wire up
        <a href="https://github.com/scrogson/oauth2">OAuth2</a> with
        <a href="https://github.com/phoenixframework/phoenix">Phoenix</a> specifically for
        Google's implementation of the API.
      </p>

      <br>
      <br>

      <%= if @current_user do %>
        <h2>Hello <%= @current_user.name %>.</h2>
        <img src="<%= @current_user.avatar %>" class="img-circle"/>
        <%= link "Logout", to: auth_path(@conn, :delete), method: :delete, class: "btn btn-danger" %>
      <% else %>
        <br>
        <br>

        <a class="btn btn-primary btn-lg" href="<%= auth_path @conn, :index, "google" %>">
          <i class="fa fa-google"></i>
          Sign in with Google
        </a>
      <% end %>
    </div>

    <div class="container-fluid">
      <%= @inner %>
    </div>
    <script src="<%= static_path(@conn, "/js/app.js") %>"</script>
    <script>require("/web/static/app")</script>
  </body>
</html>
